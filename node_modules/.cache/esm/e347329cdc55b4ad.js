let APP_PORT,DB_URL,routes,errorHandler,mongoose;_dc5‍.w("./config",[["APP_PORT",["APP_PORT"],function(v){APP_PORT=v}],["DB_URL",["DB_URL"],function(v){DB_URL=v}]]);_dc5‍.w("./routes",[["default",["routes"],function(v){routes=v}]]);_dc5‍.w("./middlewares/errorHandler",[["default",["errorHandler"],function(v){errorHandler=v}]]);_dc5‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);const express = require('express')
const app = express()






// const mongoose = require("mongoose");

// Database connection
mongoose.connect(DB_URL, {
    useNewUrlParser: true,
    // useFindAndModify: false,
    useUnifiedTopology: true
});
const db = mongoose.connection;
db.on('error', _dc5‍.g.console.error.bind(_dc5‍.g.console, 'connection error:'));
db.once('open', () => {
    console.log('DB connected...');
});



app.use(express.json());         //use for enable json data  
app.use('/api', routes);

app.use(errorHandler);
app.listen(APP_PORT, () => {
    console.log(` app listening on port ${APP_PORT}`)
})
