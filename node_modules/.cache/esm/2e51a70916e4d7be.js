let JWT_SECRET,jwt;_16e‍.x([["default",()=>_16e‍.o]]);_16e‍.w("../config",[["JWT_SECRET",["JWT_SECRET"],function(v){JWT_SECRET=v}]]);_16e‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);


class JwtService {
    static sign(payload, expiry = '1y', secret = JWT_SECRET) {
        return jwt.sign(payload, secret, { expiresIn: expiry });
    }

    // static verify(token, secret = JWT_SECRET) {
    //     return jwt.verify(token, secret);
    // }
}

_16e‍.d(JwtService);