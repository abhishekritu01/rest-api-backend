let APP_PORT,DB_URL,routes,errorHandler,mongoose,path;_329‍.w("./config",[["APP_PORT",["APP_PORT"],function(v){APP_PORT=v}],["DB_URL",["DB_URL"],function(v){DB_URL=v}]]);_329‍.w("./routes",[["default",["routes"],function(v){routes=v}]]);_329‍.w("./middlewares/errorHandler",[["default",["errorHandler"],function(v){errorHandler=v}]]);_329‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_329‍.w("path",[["default",["path"],function(v){path=v}]]);const express = require('express')
const app = express()







// const mongoose = require("mongoose");

// Database connection
mongoose.connect(DB_URL, {
    useNewUrlParser: true,
    // useFindAndModify: false,
    useUnifiedTopology: true
});
const db = mongoose.connection;
db.on('error', _329‍.g.console.error.bind(_329‍.g.console, 'connection error:'));
db.once('open', () => {
    console.log('DB connected...');
});

global.appRoot = path.resolve(__dirname);
app.use(express.urlencoded({extended:false}));




app.use(express.json());         //use for enable json data  
app.use('/api', routes);
app.use('/uploads',express.static('uploads'))

app.use(errorHandler);
app.listen(APP_PORT, () => {
    console.log(` app listening on port ${APP_PORT}`)
})
